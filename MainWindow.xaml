<Window x:Class="MarkdownUtilsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Markdown Utils" Height="720" Width="1200" MinHeight="500" MinWidth="800">
    <DockPanel LastChildFill="True" Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Name="NewButton" Width="100" Margin="0,0,10,0" Padding="10,6" Click="NewButton_Click">New</Button>
            <Button Name="SaveButton" Width="120" Margin="0,0,10,0" Padding="10,6" Click="SaveButton_Click">Save .md</Button>
            <Button Name="ImportButton" Width="120" Margin="0,0,10,0" Padding="10,6" Click="ImportButton_Click">Import .md</Button>
            <Button Name="ExportButton" Width="140" Margin="0,0,10,0" Padding="10,6" Click="ExportButton_Click">Export .docx</Button>
            <TextBlock Name="StatusText" VerticalAlignment="Center" Foreground="#555">Ready</TextBlock>
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock FontWeight="SemiBold" Margin="0,0,0,6">Raw Markdown</TextBlock>
                    <WrapPanel Grid.Row="1" Margin="0,0,0,6">
                        <Grid Margin="0,0,6,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Button Name="HeadingApplyButton"
                                    Grid.Column="0"
                                    Padding="8,4"
                                    MinWidth="40"
                                    BorderThickness="1,1,0,1"
                                    Click="HeadingApplyButton_Click">H2</Button>
                            <ToggleButton Name="HeadingDropdownButton"
                                          Grid.Column="1"
                                          Padding="6,4"
                                          BorderThickness="1"
                                          Click="HeadingDropdownButton_Click">▼
                                <ToggleButton.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="H1" Tag="1" Click="HeadingLevelMenuItem_Click" />
                                        <MenuItem Header="H2" Tag="2" Click="HeadingLevelMenuItem_Click" />
                                        <MenuItem Header="H3" Tag="3" Click="HeadingLevelMenuItem_Click" />
                                        <MenuItem Header="H4" Tag="4" Click="HeadingLevelMenuItem_Click" />
                                        <MenuItem Header="H5" Tag="5" Click="HeadingLevelMenuItem_Click" />
                                        <MenuItem Header="H6" Tag="6" Click="HeadingLevelMenuItem_Click" />
                                    </ContextMenu>
                                </ToggleButton.ContextMenu>
                            </ToggleButton>
                        </Grid>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="BoldButton_Click">Bold</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="ItalicButton_Click">Italic</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="StrikethroughButton_Click">Strike</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="InlineCodeButton_Click">Inline Code</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="CodeBlockButton_Click">Code Block</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="HorizontalRuleButton_Click">Rule</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="BulletListButton_Click">Bullet List</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="NumberedListButton_Click">Numbered List</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="QuoteButton_Click">Quote</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="LinkButton_Click">Link</Button>
                        <Button Margin="0,0,6,6" Padding="8,4" Click="ImageButton_Click">Image</Button>
                    </WrapPanel>
                    <TextBox Name="EditorTextBox"
                             Grid.Row="2"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             TextWrapping="NoWrap"
                             FontFamily="Consolas"
                             FontSize="14"
                             TextChanged="EditorTextBox_TextChanged"
                             PreviewKeyDown="EditorTextBox_PreviewKeyDown"/>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#EEE" />

            <Border Grid.Column="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock FontWeight="SemiBold" Margin="0,0,0,6">Formatted Preview (Read Only)</TextBlock>
                    <WebBrowser Name="PreviewBrowser" Grid.Row="1"/>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>
